import { Button } from "@material-ui/core";

export const LoginButton = () => (
    <div className="login-button-ui-component">
        <Button variant="contained" color="primary" href="asdf">
            Login
        </Button>
    </div>
);
